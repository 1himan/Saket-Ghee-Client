/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // domains: ["t4.ftcdn.net", "example.com", "another-domain.com"], // List all domains you want to allow
  },
};

export default nextConfig;
